appId: 'aus.foop'
productName: 'Foop'
linux:
  artifactName: Foop_Setup_Linux_${arch}.${ext}
  target:
    - target: appImage
      arch:
        - x64
  category: network
mac:
  artifactName: Foop_Setup_macOS_${arch}.${ext}
  target:
    - target: dmg
      arch:
        - x64
nsis:
  artifactName: Foop_Setup_Windows_${arch}.${ext}
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true